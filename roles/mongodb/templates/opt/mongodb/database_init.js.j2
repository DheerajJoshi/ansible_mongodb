graylog = db.getSiblingDB("{{ mongodb.database }}")
graylog.createUser(
  {
    user: "{{ graylog.mongodb_username }}",
    pwd: "{{ graylog.mongodb_password }}",
    roles: [
      { role: "readWrite", db: "{{ mongodb.database }}" },
      { role: "dbAdmin", db: "{{ mongodb.database }}" }
    ]
  }
)
